name: Release

on:
  push:
    tags: ['v*', '[0-9]+.[0-9]+.[0-9]+']

permissions:
  contents: write

jobs:
  release:
    uses: dinoki-ai/osaurus-tools/.github/workflows/build-plugin.yml@master
    secrets: inherit